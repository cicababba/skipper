{
  "name": "@skipper/cli",
  "version": "1.16.3",
  "private": true,
  "bin": {
    "skipper": "./dist/skipper.bundle.cjs"
  },
  "scripts": {
    "build": "tsc && pnpm bundle",
    "bundle": "node build/bundle.mjs",
    "dev": "tsc --watch",
    "lint": "eslint src/",
    "test": "vitest run --passWithNoTests"
  },
  "dependencies": {
    "@skipper/core": "workspace:*",
    "@skipper/shared": "workspace:*",
    "commander": "^13.1.0"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  }
}
